CONFIG_LWIP_L2_TO_L3_COPY=y
CONFIG_LWIP_IP_FORWARD=y
CONFIG_LWIP_IPV4_NAPT=y

# needed for ESP-WIFI-MESH. TCP packets have 54 byte overhead, 1472 is max payload size for ESP-WIFI-MESH
CONFIG_LWIP_TCP_MSS=1418